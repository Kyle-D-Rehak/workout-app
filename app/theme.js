import { extendTheme } from "@chakra-ui/react";

const theme = {
  config: {
    initialColorMode: "dark",
    userSystemeColorMode: false,
  },
  styles: {
    global: {},
  },
};

export default extendTheme(theme);
